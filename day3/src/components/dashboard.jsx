import React from 'react';

const Dashboard = ({word}) => {
    return(
        <div>
            <h1>What's the word?</h1>
            <p>{word}</p>

        </div>
    );
}
export default Dashboard;